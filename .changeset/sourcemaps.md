---
'ferry': patch
---

Ship external sourcemaps for every dist bundle (ESM, CJS, and the CDN global) so stack traces from the published package map back to the source. Tarball audit updated to expect the .map files.
