---
'ferry': minor
---

Add `useClipboard()` React hook via the new `ferry/react` subpath export. Wraps `copyToClipboard` with copied/error state and an optional auto-reset timeout. React >=17 is an optional peer dependency.
